{
  "name": "steakbros",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "set NEXT_DIST_DIR=.next-dev&& next dev --webpack --hostname localhost",
    "build": "set NEXT_DIST_DIR=.next&& next build",
    "start": "set NEXT_DIST_DIR=.next&& next start --hostname localhost",
    "lint": "eslint ."
  },
  "dependencies": {
    "gsap": "^3.15.0",
    "next": "^16.2.3",
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  }
}
